{
  "name": "gmail",
  "version": "1.1.0",
  "description": "Wrapper for Gmail with native notifications and badge",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "pack": "electron-builder --dir",
    "release": "electron-builder",
    "dist": "electron-builder --config ./builderconfig.js"
  },
  "author": "Rob McVey",
  "license": "ISC",
  "devDependencies": {
    "dotenv": "^16.0.1",
    "electron": "^19.0.1",
    "electron-builder": "^23.0.3",
    "electron-notarize": "^1.2.1"
  }
}
